---
"@montekkundan/opendungeon": patch
---

Add a live multiplayer action log to `opendungeon-host`, expose it through `/state` and `/actions`, stream local player actions from terminal clients, and show the log in the host page plus the logged-in GM console.
